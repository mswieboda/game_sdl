{"id":"proj-12h","title":"Implement Macro-Based Asset Mapping & Lazy-Loading Weak Cache","description":"Refactor TextureManager and AudioManager to support a local map_assets macro that accepts a Named Tuple. Generate SpriteID/AudioID enums and lazy-load assets with WeakRef cache.","notes":"Technical summary of work: Migrated the framework asset ecosystem (TextureManager, AudioManager, FontManager, TileMapManager) to Symbol-based registries and caches. Replaced internal enums and raw String-fallback keys. Implemented a dual-layer resolution bridge in .get(id : Symbol) for backwards-compatibility. Created find_symbol mapping to resolve parsed tileset string names case-insensitively. Added delegating GSDL::Audio constructor with pointer safety tracking to prevent double-frees. Refactored and verified all 66+ examples under examples/ to compile successfully with 100% success, resolving legacy string key usages and correcting HUDPerformance align argument to h_align.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-24T00:02:51.660838Z","created_by":"matt","updated_at":"2026-05-24T01:34:13.564803Z","closed_at":"2026-05-24T01:34:13.564589Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-2yo","title":"Add Raycasting and A* Pathfinding","description":"### Technical Requirements\n- `GSDL::Physics::Raycast`: Add `raycast(start : Point, end : Point)` to `SpatialGrid` to return the first `Collidable` hit.\n- Implement DDA (Digital Differential Analyzer) algorithm for efficient grid traversal.\n- `GSDL::Navigation::AStar`: Implement A* on a grid or graph.\n- Provide helper `Navigation.find_path(start, end, spatial_grid)` for TileMap-based pathfinding.\n- Support \"cost\" mapping (e.g., water tiles are more expensive to cross).","status":"open","priority":3,"issue_type":"feature","created_at":"2026-04-28T23:40:06.162556Z","created_by":"matt","updated_at":"2026-04-28T23:40:06.162556Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-3hy","title":"Implement UI Containers (VBox, HBox, Grid)","description":"### Technical Requirements\n- Create `GSDL::UI::Container` base class with `reflow` logic.\n- `GSDL::UI::VBox`: Automatically positions children vertically with `spacing` and `alignment` (Top, Center, Bottom).\n- `GSDL::UI::HBox`: Automatically positions children horizontally with `alignment` (Left, Center, Right).\n- `GSDL::UI::GridContainer`: Positions children in rows and columns.\n- Ensure children can specify `expand` or `shrink` flags (similar to Godot Size Flags).","status":"open","priority":3,"issue_type":"feature","created_at":"2026-04-28T23:40:06.069494Z","created_by":"matt","updated_at":"2026-04-28T23:40:06.069494Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-3m7","title":"Smooth Camera Lerp","description":"Add lerp_speed to Camera for smooth target following.","notes":"Task was already previously completed (likely before Beads was installed and switched from Trello, and was outdated from the old GAME_CONTEXT.md file). Verified implementation in src/gsdl/core/camera.cr and updated examples/camera_modes.cr to include a toggle for lerping.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.993097Z","created_by":"matt","updated_at":"2026-04-26T06:07:50.420368Z","closed_at":"2026-04-26T06:07:50.420195Z","close_reason":"Already completed","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-3m7","depends_on_id":"proj-650.8","type":"blocks","created_at":"2026-04-25T21:37:44.112615Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-4z9","title":"Implement Post-Processing Stack","description":"### Technical Requirements\n- Create `GSDL::PostProcess` manager that handles off-screen rendering.\n- Render the scene to an intermediate `GSDL::Texture` (render target).\n- Provide built-in shaders or CPU-based effects (via SDL3 render targets) for:\n  - Vignette\n  - Color Grading (LUT support)\n  - Chromatic Aberration\n  - Simple Blur / Glow.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-04-28T23:40:06.307416Z","created_by":"matt","updated_at":"2026-04-28T23:40:06.307416Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-5dr","title":"Implement Dynamic Font Atlas","description":"Here is a complete, high-level technical specification to implement a **Dynamic, On-Demand Font Atlas** system.\n\nThis plan transitions GameSDL library's current static setup into a self-managing, row-based dynamic texture allocater that completely automates asset generation behind the scenes. Implement this new dynamic font atlas loading, and usage.\n\n---\n\n## Architecture Blueprint: The Row-Based (Shelf) Allocator\n\nInstead of recalculating a complex packing layout every time a character is added, the texture real estate is managed using a **Shelf Allocator**. The texture is divided horizontally into rows (shelves) matching the maximum height of your font size. Glyphs are appended side-by-side along a shelf until it fills up, at which point a new shelf is initialized below it.\n\n---\n\n## Step-by-Step Implementation Specification\n\n### Task 1: Update the Metadata Structures\n\nWe need a structure to track the spatial position of cached glyphs, their rendering metrics, and their lifetime information.\n\n* **Implement a `GlyphMetric` structure:**\n* `x`, `y`, `width`, `height` : `Int32` (Coordinates on the physical GPU texture).\n* `bearing_x`, `bearing_y`, `advance_x` : `Int32` (Typographical metrics for vertex positioning).\n* `last_frame_used` : `UInt64` (Timestamp matching the engine's global frame count).\n* `is_active` : `Bool` (Safety flag to protect glyphs currently visible on screen).\n\n\n* **Initialize the local cache in `Font` (formerly `FontAtlas`):**\n```crystal\n@glyph_cache = {} of Char => GlyphMetric\n\n```\n\n\n\n---\n\n### Task 2: Implement the Shelf Allocator State\n\nThe `Font` class needs to keep track of where the next glyph can be safely printed onto the texture.\n\n* **Add tracking variables to `Font`:**\n* `@texture_width`, `@texture_height` : `Int32` (Typically starting at a fixed $1024 \\times 1024$ allocation).\n* `@current_shelf_y` : `Int32` (The vertical pixel offset where the active row begins).\n* `@current_shelf_h` : `Int32` (The height of the current row, based on `font_size` + padding).\n* `@next_slot_x` : `Int32` (The horizontal pixel offset where the next glyph can be written).\n\n\n\n---\n\n### Task 3: The Frame Management Hook (\"Touching\" Glyphs)\n\nTo track what characters are visible on screen and update their aging metrics, we intercept the rendering path.\n\n1. At the **start of your main rendering frame**, reset the `is_active` state of all cached metrics:\n```crystal\n@glyph_cache.each_value { |metric| metric.is_active = false }\n\n```\n\n\n2. When a `Text` entity processes its string to build geometry, it must request characters via a new `touch_glyph(char : Char)` method on the font instance.\n3. **`touch_glyph` Logic:**\n* If the character exists in `@glyph_cache`:\n* Set `metric.is_active = true`\n* Set `metric.last_frame_used = CurrentFrameCount`\n* Return the metric.\n\n\n* If the character does **not** exist:\n* Invoke the rasterization sub-routine (Task 4).\n\n\n\n\n\n---\n\n### Task 4: On-Demand Rasterization & Eviction (The Core Loop)\n\nWhen a character is missing, it must be generated, placed on the texture, and uploaded to the GPU without introducing frame drops.\n\n```\n[Request Glyph 'G'] -> Check Cache -> Missing!\n     |\n     v\n[Check Shelf Space] -> Fits? \n     |--> YES -> Blit to @next_slot_x -> Update Texture Sub-Region -> Advance X\n     |\n     +--> NO  -> [Open New Shelf] -> Fits Vertically?\n                       |--> YES -> Update @current_shelf_y -> Blit -> Advance X\n                       |\n                       +--> NO  -> [EVICTION RUN] -> Find Oldest `is_active == false`\n                                         |\n                                         v\n                                   Overwrite coordinates -> Update Cache\n\n```\n\n#### Detailed Logic Breakdown:\n\n1. **Check Local Spatial Capacity:**\n* If `@next_slot_x + glyph_width > @texture_width`:\n* **Move to new shelf:** Set `@current_shelf_y += @current_shelf_h`.\n* Set `@next_slot_x = 0`.\n\n\n\n\n2. **Handle Texture Full (Eviction Pattern):**\n* If `@current_shelf_y + glyph_height > @texture_height`:\n* Loop through `@glyph_cache` to find entries where `is_active == false`.\n* Among those inactive elements, select the entry with the **lowest** `last_frame_used`.\n* **Hijack the Slot:** Re-use the evicted element's physical `x` and `y` coordinates for the new glyph.\n* Delete the old character key from `@glyph_cache`.\n\n\n\n\n3. **GPU Memory Synchronization:**\n* Rasterize the single character into a small, temporary CPU memory buffer using your font parser (e.g., `SDL_ttf` or raw FreeType).\n* Use **`LibSDL3.update_texture`** (passing an `SDL_Rect` built from the targeted `x, y, w, h` slot coordinates) to upload *only* that small pixel block to the main atlas texture.\n\n\n4. **Finalize Allocation:**\n* Create a new `GlyphMetric` entry mapped to the character.\n* If using the shelf path (not eviction), advance your layout pointer: `@next_slot_x += glyph_width + padding`.\n\n\n\n---\n\n### Task 5: Refactor the Global Managers for Lazy Loading\n\n* **Remove forced preloading requirements:**\n* Change `FontManager.get(name, size)` so that if the combination does not exist in its internal hash map, it instantiates the `Font` structure seamlessly on the spot.\n\n\n* **Empty Texture Initialization:**\n* When `Font` initializes, instantiate a completely blank, black pointer target using `LibSDL3.create_texture` with `SDL_TEXTUREACCESS_STREAMING` or `STATIC` at $1024 \\times 1024$.\n* **Do not generate any characters yet.** The texture remains unpopulated until the first string hits a layout cycle.\n\n\n\n---\n\n## Verification Metrics Checklist\n\nWhen this feature is fully written, your performance engine logs should show the following characteristics:\n\n* **Frame Stutter Elimination:** Text object creation loops take less than `0.02ms` because they no longer wait for the system to pre-bake hundreds of unused ASCII combinations.\n* **Constant Texture Count:** Flipping between maps, menus, or dialogues keeping the same font style results in zero new texture allocations; the same GPU sheet dynamically alters its internal pixel values to match changing text patterns.","notes":"Implemented dynamic shelf-based font atlas, on-demand glyph rasterization, LRU eviction, active-glyph protection in game loop, and aligned FontInfo FFI bindings to fix texture creation memory corruption.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-23T01:45:30.029840Z","created_by":"matt","updated_at":"2026-05-23T02:36:23.650822Z","closed_at":"2026-05-23T02:36:23.650555Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-5i9","title":"Implement InputMap with Deadzones and Strength","description":"### Technical Requirements\n- Formalize `GSDL::InputMap` as a central registry for actions.\n- Allow mapping an action (Symbol) to multiple inputs (Keyboard, Mouse, Gamepad).\n- Support `action_strength(action : Symbol)` returning 0.0 to 1.0 (for analog sticks/triggers).\n- Implement configurable `deadzone` per action.\n- Add `echo` pulse detection for UI navigation (holding UP in a menu pulses the action).","status":"open","priority":3,"issue_type":"feature","created_at":"2026-04-28T23:40:06.114305Z","created_by":"matt","updated_at":"2026-04-28T23:40:06.114305Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-5tk","title":"Integrating kerning pairs into dynamic layout pipeline","description":"# Integrating kerning pairs into dynamic layout pipeline\n\nIntegrating kerning pairs into your new dynamic layout pipeline is the perfect logical next step. Since you are using `stb_truetype`, you are in luck: the library has a built-in, highly optimized function explicitly designed to query the horizontal advance offset between two glyph indices.\n\nHere is the complete technical task description to implement kerning pairs into your `Text` vertex generation loop.\n\n---\n\n## Architectural Concept: The Kerning Offset\n\nKerning is always calculated between **two specific glyphs sitting next to each other**. It cannot be pre-baked into your `GlyphMetric` struct because the offset changes depending on both the *previous* character and the *current* character.\n\nWhen generating your text geometry, you will look ahead (or look back) one character, ask `stb_truetype` for the adjustment value, and apply that directly to your cursor positioning before laying down the vertex coordinates.\n\n---\n\n## Step-by-Step Implementation Specification\n\n### Task 1: Expose the Kerning Query in `Font` (or `FontAtlas`)\n\n`stb_truetype` operates on raw internal glyph indices (not standard `Char` or Unicode values). To get the kerning, you must first convert your Crystal `Char` characters into `stb_truetype` glyph IDs, then query the layout engine.\n\n1. **Add a `get_kerning` method to your `Font` class:**\nThis method takes the previous character and the current character, converts them to glyph IDs, and returns the scaled horizontal offset.\n\n```crystal\n# Inside your Font / FontAtlas class\ndef get_kerning(prev_char : Char, curr_char : Char) : Float32\n  # 1. Convert Crystal Chars to stb_truetype glyph indices\n  # (Assuming @font_info holds your initialized stbtt_fontinfo struct)\n  glyph_prev = LibSTBTT.find_glyph_index(pointerof(@font_info), prev_char.ord)\n  glyph_curr = LibSTBTT.find_glyph_index(pointerof(@font_info), curr_char.ord)\n\n  if glyph_prev == 0 || glyph_curr == 0\n    return 0.0f32\n  end\n\n  # 2. Query raw unscaled design units from stb_truetype\n  raw_kerning = LibSTBTT.get_glyph_kern_advance(pointerof(@font_info), glyph_prev, glyph_curr)\n\n  # 3. Scale the design units to your target font size pixels\n  # (@font_scale is the value returned by stbtt_ScaleForPixelHeight)\n  return raw_kerning.to_f32 * @font_scale\nend\n```\n\n---\n\n### Task 2: Update the `Text` Geometry Pass (The Lookback Loop)\n\nYour text layout loop (where you loop through characters to construct your batch vertices) needs to track the history of the string to know what character came before the current one.\n\n1. **Modify your vertex generation loop in your `Text` class:**\nInitialize a `prev_char` tracking variable to `nil`. As you iterate over each character, apply the kerning calculation if a predecessor exists.\n\n```crystal\n# Inside your Text entity's geometry generation method\ndef update_vertices\n  # ... your initialization code ...\n  cursor_x = 0.0f32\n  cursor_y = 0.0f32\n  \n  prev_char : Char? = nil\n\n  @text.each_char do |char|\n    # Handle newlines or control characters first\n    if char == '\\n'\n      # ... reset cursor_x, advance cursor_y ...\n      prev_char = nil\n      next\n    end\n\n    # 1. FETCH METRICS & TOUCH GLYPH (Your dynamic atlas pass from earlier)\n    metric = @font_atlas.touch_glyph(char, Graphics.frame_count)\n\n    # 2. APPLY KERNING (The New Step)\n    if p_char = prev_char\n      # Ask the atlas to calculate the pixel offset for this specific pair\n      kern_offset = @font_atlas.get_kerning(p_char, char)\n      cursor_x += kern_offset\n    end\n\n    # 3. BUILD GEOMETRY\n    # Use cursor_x + metric.bearing_x to align your vertex positions\n    # ... append positions, colors, and UVs to your local array ...\n\n    # 4. ADVANCE CURSOR & UPDATE HISTORY\n    cursor_x += metric.advance_x\n    prev_char = char # Store this character for the next iteration's lookback!\n  end\nend\n```\n\n---\n\n### Task 3: Performance Check & Optimization\n\nBecause `LibSTBTT.find_glyph_index` and `LibSTBTT.get_glyph_kern_advance` perform memory lookups inside the font binary data, executing them inside a tight loop every time text vertices are updated could introduces a tiny bit of overhead.\n\nSince your `Text` class already uses a **Dirty Flag System** (`@dirty`), this math will **only run when the text string physically changes**. This means the performance impact on your engine's main frame rate is essentially zero during regular gameplay frames.\n\n* If you have highly dynamic numbers updating every frame (like a frame counter or high-frequency damage numbers), you can optionally cache common kerning pairs inside a small `Hash(Tuple(Char, Char), Float32)` on your `Font` instance to bypass the C-library lookup entirely for repeated sequences. For your baseline implementation, querying `stb_truetype` directly is perfectly acceptable.\n\n---\n\n## Verification Task\n\nTo test that your implementation is reading the kerning tables properly, create a text object displaying specific structural combinations that have dramatic structural kerning offsets in standard fonts:\n\n* **\"AVALANCHE\"** (The A and V values should visually interlock smoothly).\n* **\"WAR\"** (The A should sit snugly under the right wing of the W).\n* **\"To\"** or **\"Ye\"** (The lowercase vowel should tuck underneath the overhang of the capital letter).\n\nIf those character pairs nest cleanly together without huge awkward gaps, your engine's typographic pipeline has officially hit professional spec!","notes":"Added horizontal kerning pair support into the GSDL font rendering pipeline using stb_truetype. Bound find_glyph_index and get_glyph_kern_advance in LibSTBTrueType. Implemented caching in GSDL::Font to avoid runtime C-boundary overhead. Integrated lookback kerning offsets in calculate_width, draw_text, and generate_vertices. Resolved space-collapsing layout bug by ensuring empty glyphs like space (' ') still advance the horizontal layout cursor. Verified correctness quantitatively using pixel analysis on high-DPI screenshots, proving exact scaled pixel offsets are rendered successfully.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-23T03:07:08.772322Z","created_by":"matt","updated_at":"2026-05-23T03:30:28.342117Z","closed_at":"2026-05-23T03:30:28.341876Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-650","title":"GSDL Core Foundation","description":"Foundation mandates for the GSDL engine core architecture.","notes":"Foundation mandates for the GSDL engine core architecture. This epic covers the established, stable systems that form the GSDL framework.","status":"closed","priority":2,"issue_type":"epic","created_at":"2026-04-25T21:37:43.435262Z","created_by":"matt","updated_at":"2026-04-25T22:19:32.575073Z","closed_at":"2026-04-25T22:19:32.574910Z","close_reason":"Core foundation defined","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-650.1","title":"Core Structure","description":"Game/Scene/Entity hierarchy and execution context.","notes":"• Game owns the main loop, window, and global Draw instance.\n• Manages a stack of Scene objects (@scenes : Array(Scene)).\n• Execution Context: Game uses an internal @execution_stack to track which scene is currently processing (update/draw). Game.camera automatically resolves to the correct camera based on this context.\n• Entity System: Scene and Entity support a parent-child hierarchy via add_child. Updates and draws are automatically propagated through the hierarchy.\n• Supports push, pop, replace, and switch for scene transitions.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.480291Z","created_by":"matt","updated_at":"2026-04-25T21:45:44.694202Z","closed_at":"2026-04-25T21:37:43.480291Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.1","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.480291Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.2","title":"Asset & Data Management","description":"AssetManager (Loose/Packed) and GSDL::Data JSON store.","notes":"• AssetManager: Handles loading of assets (textures, audio, fonts, tilemaps, dialogs). Supports 'Loose' assets in debug and 'Packed' assets in release.\n• GSDL::Loadable: Unified load_assets pattern for Game and Scene.\n• Per-Scene Loading: Scenes can override load_textures, load_audio, etc., for sync or async loading.\n• GSDL::Data: JSON-centric key-value store for game state (scores, flags). Supports deep nesting and simple save/load persistence.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.527606Z","created_by":"matt","updated_at":"2026-04-25T21:45:44.749594Z","closed_at":"2026-04-25T21:37:43.527606Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.2","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.527606Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.3","title":"Rendering & Drawing","description":"Z-layered commands, vertex batching, and logical presentation.","notes":"• Command-based drawing system (DrawCommand) with z-indexing, culling, and camera-relative positioning.\n• Optimization (Layered Buffer): Commands stored in Z-index buckets (@layers) to avoid heavy sorting.\n• Depth Sorting: Within Z-layers, sorted by Y-position using a non-allocating sort! block.\n• Vertex Batching: Implement SDL_RenderGeometry to minimize draw calls for shared textures.\n• Two-Pass Tinting: Separate buffers for base textures and color-tinted overlays.\n• Logical Resolution: Decouples game resolution (e.g., 320x240) from window resolution (e.g., 1080p).\n• Scaling: Handled via SDL_SetRenderLogicalPresentation (Letterbox, Stretch, etc.).\n• Visual Offsets: GSDL::SpriteBase separates physical base (draw_y) from visual rendering position (render_offset_y) to support height/depth effects without breaking Y-sorting.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.575298Z","created_by":"matt","updated_at":"2026-04-25T21:45:44.798801Z","closed_at":"2026-04-25T21:37:43.575298Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.3","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.575298Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.4","title":"TileMap System","description":"Tiled JSON/TMX support and TileObjectFactory.","notes":"• Supports Tiled .json and .tmx formats.\n• Multi-Layer & Object Support: TileLayer and ObjectGroup layers.\n• Tileset Management: GSDL::Tileset handles textures and solid_tiles collision metadata.\n• Tile Objects: GSDL::TileObject and TileObjectFactory handle rectangles, ellipses, points, and custom tiled objects.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.622722Z","created_by":"matt","updated_at":"2026-04-25T21:45:44.845732Z","closed_at":"2026-04-25T21:37:43.622722Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.4","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.622722Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.5","title":"Collision System","description":"AABB, Circle, and SAT support via Collidable.","notes":"• Custom collision system supporting AABB (Rect), Circle, and SAT (Polygon) checks via GSDL::Collidable.\n• GSDL::CollisionSpace: Manages a flat list of Collidable entities (usually via GSDL::SceneCollisions).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.669956Z","created_by":"matt","updated_at":"2026-04-25T21:45:44.892827Z","closed_at":"2026-04-25T21:37:43.669956Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.5","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.669956Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.6","title":"UI & RichText","description":"Component suite and texture-baked RichText.","notes":"• FontManager & DialogManager (YAML-based structured dialogue).\n• UI Components: Button, ProgressBar, Slider, TabContainer, TextInput, ScrollTextView.\n• RichText: Inline colored/styled text with texture baking strategy.\n• RichTextTyped: Typewriter-style reveals for rich text.\n• HUD: Screen-space overlays with anchoring and padding support.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.715342Z","created_by":"matt","updated_at":"2026-04-25T21:45:44.941822Z","closed_at":"2026-04-25T21:37:43.715342Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.6","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.715342Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.7","title":"Input & Tweens","description":"Action mapping, MultiTapTracker, and Tweenable presets.","notes":"• Input: GSDL::Input and GSDL::Keys with action-based mapping.\n• MultiTapTracker: Handles double/triple tap detection.\n• Animation: GSDL::Animation and AnimationPlayer for frame-based sprites.\n• Particles: GSDL::ParticleEmitter and GSDL::Particle for VFX.\n• Movement Controllers: GSDL::TopDownController, PlatformerController, PhysicsController, and VerticalScrollController.\n• Tweens: GSDL::Tweenable for transitions (x, y, scale, color, tint) with presets (flash, shake, pulse, spin).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.762215Z","created_by":"matt","updated_at":"2026-04-25T21:45:44.989331Z","closed_at":"2026-04-25T21:37:43.762215Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.7","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.762215Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.8","title":"Camera System","description":"View transformation, modes, and screen shake.","notes":"• View transformation (x, y, zoom).\n• Modes: Fixed, AutoScroll, CenterOnTarget (with/without boundary), and Manual.\n• Built-in screen shake via Tweenable integration.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.810574Z","created_by":"matt","updated_at":"2026-04-25T21:45:45.034847Z","closed_at":"2026-04-25T21:37:43.810574Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.8","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.810574Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-650.9","title":"Distribution & Packaging","description":"gsdl-packer and automated OS-specific bundling.","notes":"• gsdl-packer: Standalone tool to bundle assets into assets.pack binary.\n• release packaging: Automated bundle creation for macOS (.app), Windows (.exe + DLLs), and Linux (.tar.gz).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.858851Z","created_by":"matt","updated_at":"2026-04-25T21:45:45.084279Z","closed_at":"2026-04-25T21:37:43.858851Z","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-650.9","depends_on_id":"proj-650","type":"parent-child","created_at":"2026-04-25T21:37:43.858851Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-6gd","title":"Architectural Concept: The Kerning Offset","description":"Kerning is always calculated between **two specific glyphs sitting next to each other**. It cannot be pre-baked into your `GlyphMetric` struct because the offset changes depending on both the *previous* character and the *current* character.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-23T03:06:47.922879Z","updated_at":"2026-05-23T03:06:53.917150Z","closed_at":"2026-05-23T03:06:53.916977Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-8sr","title":"Implement GSDL::UI::NinePatch","description":"### Technical Requirements\n- Create `GSDL::UI::NinePatch` that takes a texture and 4 margin offsets (Left, Right, Top, Bottom).\n- Rendering logic must divide the texture into 9 regions:\n  - 4 corners (not scaled).\n  - 4 edges (scaled in 1 dimension).\n  - 1 center (scaled in 2 dimensions).\n- Essential for UI boxes and buttons that need to scale without distorting borders.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-04-28T23:40:06.212541Z","created_by":"matt","updated_at":"2026-04-28T23:40:06.212541Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-9nd","title":"Implement GSDL::AudioPlayer2D","description":"### Technical Requirements\n- Create `GSDL::AudioPlayer2D` that tracks a world position.\n- In `update(dt)`, calculate `volume` and `pan` based on distance to the current `GSDL::Camera` focus.\n- Support attenuation curves (Linear, Inverse, Exponential).\n- Support a `max_distance` where audio becomes silent.\n- Interface with the new `volume=` and `pan=` methods in `GSDL::Audio`.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-04-28T23:40:06.259908Z","created_by":"matt","updated_at":"2026-04-28T23:40:06.259908Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-a1y","title":"Step-by-Step Implementation Specification","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-23T03:06:47.922879Z","updated_at":"2026-05-23T03:06:53.920241Z","closed_at":"2026-05-23T03:06:53.920085Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-awg","title":"Implement Volume, Pitch, and Global Sound Settings","description":"### 1. Analysis of Current Audio System\nThe current implementation in `src/gsdl/audio/` uses a custom SDL3 Mixer library (`LibSDL3Mixer`).\n- **`GSDL::Audio`**: Currently a thin wrapper around `LibSDL3Mixer::Track` and `LibSDL3Mixer::Audio`. It only supports basic `play`, `pause`, `resume`, and `stop`.\n- **`GSDL::AudioManager`**: Manages the singleton `@mixer` and the lifecycle of audio assets.\n\n### 2. Capabilities of SDL3 Mixer\nThe `LibSDL3Mixer` library (found in `../sdl3/src/sdl3/audio/mixer.cr`) is highly capable and supports the following relevant features:\n\n| Level | Setting | SDL3 Mixer Function |\n| :--- | :--- | :--- |\n| **Track** | Volume | `MIX_SetTrackGain(track, gain)` (0.0 to 1.0+) |\n| **Track** | Pitch | `MIX_SetTrackFrequencyRatio(track, ratio)` (1.0 = normal) |\n| **Track** | Looping | `MIX_SetTrackLoops(track, loops)` (-1 for infinite) |\n| **Track** | Pan | `MIX_SetTrackStereo(track, gains)` (Left/Right balance) |\n| **Track** | Categories | `MIX_TagTrack(track, tag)` (Assigns a string tag to a track) |\n| **Global** | Master Volume | `MIX_SetMixerGain(mixer, gain)` |\n| **Global** | Category Volume| `MIX_SetTagGain(mixer, tag, gain)` |\n\n### 3. Proposed Enhancements\n\n#### **A. Individual Audio Settings (`GSDL::Audio`)**\nWe can expand the `GSDL::Audio` class to include properties that directly manipulate the underlying track:\n- **`volume` / `volume=`**: Control individual gain.\n- **`pitch` / `pitch=`**: Control playback speed/pitch ratio.\n- **`looping` / `looping=`**: Set loop count or toggle infinite loops.\n- **`pan` / `pan=`**: Adjust stereo balance.\n- **`category` / `category=`**: Assign the audio to a tag (e.g., \"sfx\", \"music\").\n\n#### **B. Global Sound Settings (`GSDL::SoundSettings`)**\nI recommend introducing a new global class, `GSDL::SoundSettings`, to manage game-wide audio configuration. This class will act as a centralized hub for the mixer's global states:\n- **Master Volume**: Controls `MIX_SetMixerGain`.\n- **Category Volumes**: A mapping of category names (SFX, Music, UI, etc.) to their respective `MIX_SetTagGain` values.\n- **Stereo/Mono Toggle**: While the mixer is typically initialized in stereo, a \"Mono\" setting can be simulated by applying a 50/50 stereo mix (`MIX_SetTrackStereo`) to all tracks when the setting is enabled.\n\n### 4. Implementation Strategy\n\n1.  **Refactor `GSDL::Audio`**: Add the new property methods. When `play` is called, it should optionally ensure the correct category tag is applied.\n2.  **Create `GSDL::SoundSettings`**:\n    *   Store settings in static variables.\n    *   Provide methods like `set_volume(category, value)`.\n    *   Ensure it can persist these settings (if desired, though usually handled by a separate `SaveData` system).\n3.  **Update `GSDL::AudioManager`**:\n    *   Allow passing a category during `load` or `get`.\n    *   The `@mixer` instance needs to be accessible to `SoundSettings` to apply global gains.\n4.  **Advanced Features (Optional Discoveries)**:\n    *   **3D Audio**: `LibSDL3Mixer` supports `MIX_SetTrack3DPosition`, which could be added for spatial sound effects.\n    *   **Fading**: `MIX_PlayTrack` supports `fade_in_milliseconds` via properties, and `MIX_StopTrack` supports `fade_out_frames`.\n\n### 5. Stereo/Mono Handling\nTo implement a global \"Mono\" toggle at runtime:\n- **Option 1 (Hardware Level)**: Re-initialize the mixer with 1 channel. This is disruptive and not recommended for a settings menu.\n- **Option 2 (Software Downmix)**: When `SoundSettings.mono = true`, the `AudioManager` can iterate through all active tracks and apply a `StereoGains(left: 0.5, right: 0.5)` to collapse the stereo image into mono.\n","notes":"Implemented comprehensive audio enhancements:\n- Refactored GSDL::Audio to support volume, pitch, looping, and panning.\n- Introduced GSDL::SoundSettings for global Master/Category volume control and Mono toggle.\n- Updated AudioManager and Loadable to support category-based audio loading.\n- Added examples/audio_features.cr and examples/audio_settings.cr to demonstrate the new capabilities.\n- Verified backward compatibility with existing audio examples.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-04-28T22:58:58.632670Z","created_by":"matt","updated_at":"2026-04-28T23:12:53.099571Z","closed_at":"2026-04-28T23:12:53.099374Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-c7h","title":"Refine and Optimize RichText & RichTextTyped","description":"**Objective**: Perform a comprehensive audit of RichText and RichTextTyped to ensure their manual segment-based baking logic is robust and optimized for the oversampling architecture.\n**Details**:\n- **Oversampling Consistency**: Verify that oversample_ratio is applied uniformly across all segments and that coordinate mapping between logical layout and oversampled baking is pixel-perfect.\n- **Bake Performance**: RichTextTyped currently re-bakes the entire texture on every character/word typed. Investigate if performance impacts are significant at high oversample ratios and consider 'incremental baking' if necessary.\n- **Tag Verification**: Rigorously test style tags (<b>, <i>, c:) under various scales and rotations to ensure the custom layout engine doesn't introduce artifacts.\n- **Alignment Support**: Ensure that Center and Right alignment in RichText handles multi-line segment layouts correctly.","notes":"Optimized RichText/RichTextTyped by decoupling layout and baking passes. Introduced `Text::OversampleRatio` constant (default 4.0). Aligned Text/RichText draw math with SpriteBase/Sprite for engine-wide consistency. Verified high-quality scaling and efficient typing animations in examples.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-26T04:31:01.326930Z","created_by":"matt","updated_at":"2026-04-26T04:42:08.299447Z","closed_at":"2026-04-26T04:41:55.685894Z","close_reason":"Completed","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-ctr","title":"Spatial Querying","description":"Implement spatial partitioning for CollisionSpace optimization.","notes":"Optimized CollisionSpace grid refresh. Moved refresh_grid call from query path to Scene#update, ensuring grid is rebuilt exactly once per frame for scenes using SceneCollisions.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.950399Z","created_by":"matt","updated_at":"2026-04-28T02:05:23.675894Z","closed_at":"2026-04-27T23:54:49.576596Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-ctr","depends_on_id":"proj-650.5","type":"blocks","created_at":"2026-04-25T21:37:44.039887Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-dex","title":"Integrate Post-Initialization Scene Atlas Baking with WeakRef Cache","description":"# Technical Specification: Post-Initialization Scene Atlas Baking\n\n## Objective\n\nIntegrate our runtime texture packing (`finalize_atlas`) with the newly implemented symbol-based `WeakRef` asset cache pipeline. The architecture must allow scenes to dynamically load assets during `Scene#initialize` via standard `.get` or an explicit `.preload` step. Once initialization concludes, the engine will harvest all live textures inside the `WeakRef` cache, pack them into an optimized mega-atlas page on the GPU, and dynamically route drawing calls through the shared atlas texture.\n\n---\n\n## Architecture Flow\n\n1. **Scene Creation:** Objects (`Player`, `Tilemap`) execute `TextureManager.get(:key)` inside their constructors, creating a strong pointer to a Crystal `Texture` instance and placing it in `@@cache`.\n2. **Explicit Preloading:** Procedural/delayed assets (e.g., `:bullet`, `:explosion_particle`) are explicitly pushed to memory via a new `TextureManager.preload([...])` helper right before the scene starts.\n3. **Atlas Finalization:** `TextureManager.finalize_atlas` runs at the very end of scene loading. It harvests the outer `Texture` instances currently held in the `WeakRef` cache, copies their raw GPU pixels onto a centralized render-target atlas page, and assigns the `atlas_handle` and `atlas_rect` metadata directly onto those active texture objects.\n\n---\n\n## Step-by-Step Implementation Tasks\n\n### Task 1: Implement `TextureManager.preload`\n\nCreate a high-level array processor that forces arbitrary assets into the cache loop before baking occurs.\n\n```crystal\n# Inside module GSDL::TextureManager\ndef self.preload(symbols : Array(Symbol)) : Nil\n  @@mutex.synchronize do\n    symbols.each do |sym|\n      self.get(sym) # Automatically handles registry lookups and cache population\n    end\n  end\nend\n```\n\n---\n\n### Task 2: Refactor `finalize_atlas` to Harvest the Weak Cache\n\nModify your existing packing routine to extract active objects from `@@cache` instead of referencing the legacy `@@textures` map.\n\n```crystal\ndef self.finalize_atlas : Nil\n  @@mutex.synchronize do\n    # 1. Gather all active texture references held by the current scene\n    to_pack = [] of Texture\n    @@cache.each_value do |weak_ref|\n      if tex = weak_ref.value\n        # Only pack if it hasn't already been assigned to an atlas page\n        to_pack << tex if tex.atlas_handle.nil?\n      end\n    end\n\n    return if to_pack.empty?\n\n    # 2. Sort by height descending (Preserve existing shelf-packing logic)\n    to_pack.sort_by! { |t| -t.height }\n\n    # ... [Keep your existing POT calculations and page assignment blocks] ...\n\n    # 3. Render and Update Metadata\n    pages.each do |page|\n      atlas_tex = Texture.new(current_atlas_w, current_atlas_h, access: TextureAccess::Target)\n      Game.draw.with_target(atlas_tex) do\n        Game.draw.color = Color::Transparent\n        Game.draw.clear\n\n        page.each do |(tex, x, y)|\n          # Render the loose texture onto our target atlas sheet\n          Game.draw.texture(tex, x: x.to_f32, y: y.to_f32, draw_immediately: true)\n\n          # Update metadata directly on the instance held by game entities\n          tex.atlas_handle = atlas_tex.to_sdl\n          tex.atlas_rect = FRect.new(x: x.to_f32, y: y.to_f32, w: tex.width, h: tex.height)\n          \n          # OPTIONAL: Release the underlying raw standalone GPU texture handle \n          # here to optimize VRAM, since its data now resides on the atlas page.\n        end\n      end\n      @@atlases << atlas_tex\n    end\n  end\nend\n```\n\n---\n\n### Task 3: Update Rendering Subsystem to Intercept Atlas Metadata\n\nEnsure your draw commands transparently check if a `Texture` object has been baked into an atlas page, substituting the source coordinates and handle on the fly.\n\n```crystal\n# Inside your drawing backend / LibSDL3 rendering interface:\ndef draw_texture(tex : Texture, destination_rect : FRect)\n  if handle = tex.atlas_handle\n    # The texture is atlased! Redirect draw call to use the atlas handle and baked bounds\n    LibSDL3.render_texture(..., handle, tex.atlas_rect, destination_rect)\n  else\n    # Fallback path for loose, un-atlased textures loaded mid-game\n    LibSDL3.render_texture(..., tex.to_sdl, nil, destination_rect)\n  end\nend\n```\n\n---\n\n## Verification Requirements\n\n* **Zero-Downtime Instance Update:** Instantiated entities should receive the updated `atlas_handle` instantly. Verify that a `Player` initialized before `finalize_atlas` draws from the combined mega-atlas sheet correctly on frame 1 without needing to re-fetch its asset.\n* **Delayed / Mid-Game Fetching:** Verify that calling `TextureManager.get(:new_item)` *after* `finalize_atlas` has run safely bypasses the atlas system, cleanly falling back to loading a loose raw texture file without breaking the game or crashing.\n","notes":"Integrated post-initialization scene atlas baking with WeakRef cache. Implemented private reentrant helper get_internal to prevent preloading deadlocks. Updated finalize_atlas to harvest the WeakRef cache, and updated immediate-mode drawing in Draw to route atlas textures transparently. Verified with full library and sprite example builds and execution runs.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-24T02:43:45.172998Z","created_by":"matt","updated_at":"2026-05-24T02:56:30.234504Z","closed_at":"2026-05-24T02:56:30.234248Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-dfm","title":"Implement Sound Instance Pooling & Channel Management","description":"## Objective\nImplement a multi-channel sound instance pool for the `AudioManager`. Currently, audio assets are cached cleanly using symbols and `WeakRef` logic, but playing a sound effect should not interrupt an already-playing instance of that same sound (e.g., rapid-fire lasers or overlapping explosions). The engine needs a system to dynamically allocate and track active audio channels, reusing them efficiently without clipping active audio.\n\n## Step-by-Step Implementation Tasks\n\n### Task 1: Initialize the SDL_mixer Channel Pool\n* In your engine's audio initialization phase, explicitly allocate the channel pool size (typically 32).\n```crystal\nMix.allocate_channels(32)\n```\n\n### Task 2: Implement Channel Reservation & Playback Logic\nRefactor the playback method to:\n1. Fetch the audio asset from the `WeakRef` cache.\n2. Pass `-1` to the channel argument of `Mix.play_channel` to automatically find an open channel.\n3. Handle the fallback case where all channels are full.\n\n### Task 3: Implement Instance Control (Stop, Pause, Fade)\n* Ensure `play_sound` returns the allocated `Int32` channel ID.\n* Implement `stop_channel(channel_id : Int32)` and `fade_out_channel(channel_id : Int32, duration_ms : Int32)`.\n\n### Task 4: Add Group/Tag Controls (Optional Quality-of-Life)\n* Reserve a specific range of channels for Sound Effects (SFX) and another for Music/Ambient loops using `Mix.group_channel`.\n\n## Verification Requirements\n* **Overlapping Playback Test:** Trigger the same sound effect 5 times rapidly. Verify they overlap simultaneously.\n* **Volatile Cache Safety:** Ensure playback holds a strong reference to the asset to prevent GC while mixing.\n","notes":"Implemented Sound Instance Pooling with a 32-track pool. Divided tracks into SFX (0-23) and Music (24-31) ranges. Added AudioManager.play_sound, stop_channel, and fade_out_channel. Verified overlapping playback with examples/audio_pooling.cr. Guaranteed cache safety by holding strong references in @@track_owners during playback.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-24T01:53:48.949516Z","created_by":"matt","updated_at":"2026-05-24T02:02:49.900290Z","closed_at":"2026-05-24T02:02:49.900110Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-dz0","title":"Optimize Dynamic Text Components","description":"**Objective**: Ensure that components with frequently changing text (TextTyped, NumberFlash, TextBox) are optimized to prevent performance degradation from excessive texture re-baking.\n**Details**:\n- **Bake Throttle**: For components like TextTyped, verify that the re-baking frequency is tied correctly to the typing speed and doesn't waste cycles.\n- **Size Stability**: Audit NumberFlash to ensure that rapidly changing numbers (e.g., '100' -> '1000') don't cause jarring layout shifts due to changing baked texture dimensions.\n- **Text Selection/Input**: Verify TextBox and TextInput interaction logic (cursor placement, selection boxes) now that they must map logical mouse coordinates back to an oversampled texture layout.","notes":"Optimized dynamic text components. TextTyped now uses `visible_characters` to avoid re-baking during animation. NumberFlash uses the new `opacity` property to fade out via texture alpha mod instead of color changes. Added `cursor_pos` helper to Text for accurate logical coordinate mapping. Verified with text_input and full examples.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-26T04:31:01.383768Z","created_by":"matt","updated_at":"2026-04-26T05:05:25.229746Z","closed_at":"2026-04-26T05:05:16.850598Z","close_reason":"Completed","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-epq","title":"Fix Rect and FRect clipping/batching state desynchronization in Draw","description":"Fix GSDL::Draw's state desynchronization in render loop where flush_batch modifies hardware state but local tracking variables (scale, clip_rect) are not updated. Also implement clipped_out? for all other command types.","notes":"Resolved state desynchronization in GSDL::Draw#draw by updating tracking variables active_scale_x, active_scale_y, and active_clip_rect immediately after flush_batch. Implemented clipped_out? culling checks for DrawFRectsCommand, DrawPointCommand, DrawPointsCommandBase, and DrawLineCommand to optimize shape rendering.","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-05-28T01:48:36.333574Z","created_by":"matt","updated_at":"2026-05-28T01:52:22.442818Z","closed_at":"2026-05-28T01:52:22.442610Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-fc9","title":"Asset Mapping Registry + Lazy-Loading Weak Cache","description":"Migrate the asset management pipeline (`TextureManager`, `FontManager`, `AudioManager`, `TileMapManager`) away from rigid, manual file preloading. Instead, implement a two-tier architecture:\n\n1. A **Permanent Static Registry** that maps a clean, short enum key (e.g., `.player`) to its physical file path at startup without loading pixels/bytes.\n2. A **Volatile Weak Cache** (`WeakRef`) that lazily loads the asset file into memory the very first time it is requested by an entity, automatically freeing it via Crystal's Garbage Collector when no longer in use.\n3. An optional `preload: true` flag during registration for scenarios where the developer explicitly wants to bypass lazy-loading and force-allocate the asset upfront.\n\n---\n\n## Step-by-Step Implementation Tasks\n\n### Task 1: Define the Asset Identifier Enums\n\nCreate central, strongly typed enums for your asset types to completely eliminate loose string paths from entity code.\n\nExample:\n\n```crystal\nmodule Assets\n  enum Texture\n    Player\n    EnemyGoblin\n    BackgroundMenu\n    # ... Add other baseline sprites as examples\n  end\n\n  enum Audio\n    Jump\n    Hurt\n    MenuSelect\n  end\n\n  # ... other enums for Font, TileMap\nend\n```\n\n---\n\n### Task 2: Implement the Two-Tier Asset Manager (e.g., `TextureManager`)\n\nRefactor the target managers to maintain both the immutable path mapping registry and the dynamic weak reference lookup cache.\n\n*Implement the following structure (using `TextureManager` as the blueprint; repeat for `AudioManager`):*\n\n```crystal\nclass TextureManager\n  # 1. The Immutable Registry (Maps Enum Key -> String Path)\n  @registry = {} of Assets::Texture => String\n  \n  # 2. The Volatile Cache (Maps Enum Key -> Weak Ref of Loaded Texture)\n  @cache = {} of Assets::Texture => WeakRef(Texture)\n\n  # Registration Pass (Called at startup / scene initialization)\n  def self.register(id : Assets::Texture, path : String, preload : Bool = false)\n    @registry[id] = path\n\n    if preload\n      # Immediately force a load into the weak cache\n      self.get(id)\n    end\n  end\n\n  # Safe, Lazy-Loading Fetch Pass\n  def self.get(id : Assets::Texture) : Texture\n    # Step A: Check if the texture is already alive in memory\n    if weak_ref = @cache[id]?\n      if texture = weak_ref.value\n        return texture\n      end\n    end\n\n    # Step B: Not in memory. Look up its registered file path\n    path = @registry[id]? || raise \"Asset Registry Error: Assets::Texture #{id} was never registered!\"\n\n    # Step C: Lazily load the asset on-demand right now\n    new_texture = Texture.new(path)\n    \n    # Step D: Cache it wrapped safely inside a WeakRef\n    @cache[id] = WeakRef.new(new_texture)\n    \n    return new_texture\n  end\n\n  # Housekeeping Maintenance Pass (Call during scene transitions)\n  def self.prune_dead_references\n    @cache.select! do |key, weak_ref|\n      # Keep the key entry only if the underlying texture hasn't been GC'd\n      !weak_ref.value.nil?\n    end\n  end\nend\n```\n\n---\n\n### Task 3: Migrate Preloading Blocks to the Configuration Registry\n\nLocate your engine's central setup locations (such as the main `Game` class initialization or individual `Scene#load` routines) where string-based preloading used to occur. Convert them to the non-blocking registration step.\n\n```crystal\n# Inside Game initialization or Scene loading pass:\nTextureManager.register(.player, \"assets/sprites/characters/hero.png\")\nTextureManager.register(.background_menu, \"assets/ui/main_menu.png\", preload: true) # Bypasses lazy-loading for critical UI\n\nAudioManager.register(.jump, \"assets/audio/fx/jump.wav\")\n```\n\n---\n\n### Task 4: Refactor Entity Callsites to Omit Namespace\n\nUpdate all entity, component, and sprite constructors to accept the strongly typed enums. Because Crystal can infer the enum type from the method signature, make sure the callsites use the clean leading-dot (`.`) syntax.\n\n#### Updated Component API:\n\n```crystal\nclass Sprite < Component\n  # Update constructor to take the strongly typed enum\n  def initialize(id : Assets::Texture)\n    @texture = TextureManager.get(id)\n  end\nend\n```\n\n#### Clean Consumer Syntax:\n\n```crystal\nclass Player < Entity\n  def initialize\n    # Zero directory structures, zero string typos, full compile-time safety\n    @sprite = Sprite.new(.player)\n    @jump_sound = AudioManager.get(.jump)\n  end\nend\n```\n\n---\n\n## Verification Requirements\n\n* **Compile-Time Typos:** Verify that attempting to write `TextureManager.get(.playr)` throws a native Crystal compilation error rather than failing silently or breaking at runtime.\n* **Automatic Resource Cleanup:** Verify that if an entity using `.player` is deleted, running a Garbage Collection check removes the texture instance, and requesting `.player` again in a later scene safely re-reads the registry configuration and re-loads the texture without dropping an error.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-23T23:45:13.405999Z","created_by":"matt","updated_at":"2026-05-24T00:02:53.772470Z","closed_at":"2026-05-24T00:02:53.772212Z","close_reason":"Completed","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-jxk","title":"Graphics: Unified Runtime Texture Atlas","description":"Goal: Consolidate all individual textures into one or more master GPU textures at runtime. This maximizes batching efficiency while optimizing VRAM usage by using smart Power-of-Two (POT) scaling.\n\n## Technical Specifications\n\n### 1. Affected Files\n- src/gsdl/gfx/texture.cr: Add atlas_rect : FRect? and atlas_handle : SDL3::Texture?.\n- src/gsdl/gfx/texture_manager.cr: Implement finalize_atlas logic, shelf-packing, and master texture storage.\n- src/gsdl/gfx/draw.cr: Update add_texture_to_batch to utilize atlas UV coordinates.\n- src/gsdl/core/game.cr: Trigger TextureManager.finalize_atlas after load_assets in the _init sequence.\n\n### 2. Implementation Requirements\n- Smart Sizing: The atlas should start at the smallest possible POT size (e.g. 512x512) and grow dynamically (1024x1024, 2048x1024, etc.) based on the results of the packing algorithm.\n- Hardware Limits: Use SDL_GetRendererInfo to respect the GPU's max_texture_width and max_texture_height. If assets exceed this limit, spawn additional atlas pages.\n- Padding: Include 2px of transparent padding between assets to prevent texture bleeding.\n- Universal Mode: Runs in both Debug and Release modes.\n\n### 3. UV Calculation Logic\nIn Draw#add_texture_to_batch, normalize coordinates against the specific atlas page handle:\n\n```crystal\nif a_rect = command.texture.atlas_rect\n  atlas_handle = command.texture.atlas_handle.not_nil!\n  atlas_w, atlas_h = atlas_handle.size\n  \n  base_x, base_y = a_rect.x, a_rect.y\n  \n  if src = command.source_rect\n    u1 = (base_x + src.x) / atlas_w\n    v1 = (base_y + src.y) / atlas_h\n    u2 = (base_x + src.x + src.w) / atlas_w\n    v2 = (base_y + src.y + src.h) / atlas_h\n  else\n    u1 = base_x / atlas_w\n    v1 = base_y / atlas_h\n    u2 = (base_x + a_rect.w) / atlas_w\n    v2 = (base_y + a_rect.h) / atlas_h\n  end\nend\n```\n\n## Acceptance Criteria\n- [ ] TextureManager produces the smallest possible POT atlas that fits all assets.\n- [ ] Batcher correctly identifies sub-textures and switches atlas pages only when the master handle changes.\n- [ ] Zero visual regressions in texture rendering compared to individual texture loading.","notes":"Implemented dynamic runtime texture atlas packing (shelf algorithm). Updated Draw and TextureManager for atlas-aware rendering and batching. Verified via performance_stress.cr (significant flush count reduction).","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-27T21:43:02.757675Z","created_by":"matt","updated_at":"2026-04-27T22:37:39.720228Z","closed_at":"2026-04-27T22:37:39.720051Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-kfk","title":"Performance: Batch Flush vs. Command Metrics","description":"Goal: Differentiate between total draw calls (API requests) and actual hardware 'flushes' (when texture changes or buffer capacity is reached).\n\n## Implementation\n- Add @current_flush_count and @last_flush_count to GSDL::Draw.\n- Increment flush count inside Draw#flush_batch (only if vertex buffer is non-empty).\n- Display 'flushes' alongside 'commands' in the GSDL::HUDPerformance overlay (src/gsdl/ui/hud.cr).\n\n## Success Metric\nHealthy batching should show flushes significantly lower than commands in standard scenes.","notes":"Extended implementation to include Commands and Flushes in the GSDL::Performance session report and rolling averages in HUDPerformance (when performance_monitoring_enabled is true). Verified with performance_stress.cr.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-27T21:37:12.593181Z","created_by":"matt","updated_at":"2026-04-27T22:18:07.002383Z","closed_at":"2026-04-27T21:56:25.153261Z","close_reason":"Completed","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-l28","title":"UI Layout & Alignment Audit","description":"**Objective**: Verify that high-level UI components correctly align, scale, and pad their internal baked Text objects.\n**Details**:\n- **Centering Accuracy**: Audit Button and Message centering logic. Since baked textures may have slightly different dimensions than the logical font metrics, verify that visual centering remains perfect.\n- **Wrapping & Resizing**: Verify DialogBox, Notification, and HUD correctly respond to text changes that trigger dimension updates in the underlying baked textures.\n- **Logical Presentation**: Ensure HUD elements remain crisp when the game's logical presentation or window size changes, verifying that they don't accidentally re-bake using the wrong DPI or scale factor.","notes":"Completed comprehensive audit of UI components (Button, Message, DialogBox, Notification, HUD). All components correctly leverage the logical dimensions of the new baked-text system. Introduced `visible_characters` and `opacity` to base Text class to optimize dynamic UI elements (TextTyped, NumberFlash), significantly reducing re-bake frequency. Verified pixel-perfect alignment and scaling across all UI examples.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-26T04:31:01.429631Z","created_by":"matt","updated_at":"2026-04-26T05:56:35.527496Z","closed_at":"2026-04-26T05:56:27.401633Z","close_reason":"Completed","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-px3","title":"Add ParallaxLayer and Background system","description":"### Technical Requirements\n- Create `GSDL::ParallaxLayer` that takes a texture and a `scroll_scale` (Point).\n- Implement tiling logic in `ParallaxLayer#draw(draw, camera)`.\n- Use `camera.x * scroll_scale.x` to calculate the visual offset.\n- Add `GSDL::Background` class to manage a stack of ParallaxLayers.\n- Support infinite tiling (wrapping) and autoscroll velocity independent of the camera.","status":"open","priority":3,"issue_type":"feature","created_at":"2026-04-28T23:40:06.021940Z","created_by":"matt","updated_at":"2026-04-28T23:40:06.021940Z","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-svz","title":"SDF Font Support by Default","description":"Enable Signed Distance Field (SDF) rendering by default for all fonts loaded via GSDL to ensure high-fidelity text at any scale.","notes":"Enabled SDF rendering by default for all fonts loaded via GSDL::FontManager. Updated load, load_from_memory, and get methods to explicitly set font.sdf = true. Verified with a test script and by building examples/full.cr.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T22:05:33.910383Z","created_by":"matt","updated_at":"2026-04-25T22:25:00.234812Z","closed_at":"2026-04-25T22:25:00.234644Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-tym","title":"Scene Persistence & Snapshot Engine","description":"Implement a scene-specific persistence system to capture dynamic game state (e.g., entity positions, health, local variables) distinct from global game metadata.\n\n## Architectural Approach\n- Implement a `GSDL::Saveable` module that can be mixed into entities or components that need to be persisted.\n- Entities must implement a `save_state : Hash(String, JSON::Any)` method for serialization.\n- Extend `SceneManager` to traverse dynamic entities and collect their state using the `Saveable` interface.\n- During save operations, `SceneManager` will ask each registered \"Saveable\" entity to serialize itself and merge the result with the current room ID into a \"Scene Snapshot\" object.\n- Structure the persistence payload to contain both global data (from `GameData`) and transient scene snapshots:\n  {\n    \"global\": { ... }, \n    \"scene\": { \"room_id\": \"...\", \"entities\": { ... } }\n  }\n- Utilize the existing binary persistence logic (Zlib + XOR + CRC32) for the full snapshot.\n\n## Requirements\n- Add a new `save_scene_state` example to `examples/` demonstrating entity state capture, saving, and reloading.\n- Ensure state loading can reconstruct entity positions and local variables within a scene.\n- Maintain backward compatibility where scenes without state are handled gracefully.","notes":"# Architectural Summary: Scene Persistence Strategy\n\n## Decision\n- We will keep the library core lean and avoid forcing `GSDL::Saveable` on internal or non-state classes (e.g., textures, engine internals, shaders).\n- Future scalability relies on consumers including `GSDL::Saveable` in their own project entities and overriding `save_state`/`load_state` on an as-needed basis.\n\n## Rationale\n- Implementing `Saveable` globally creates significant maintenance debt, schema versioning issues, and dead code for users not utilizing persistence.\n- Monolithic serialization (forcing everything to be saveable) would make versioning and schema migrations nearly impossible to manage long-term.\n\n## Developer Best Practices\n- **Selective Implementation**: Only implement `Saveable` for dynamic game-state classes (e.g., Players, NPCs, interactive objects).\n- **Versioning**: Avoid hard-coding schemas across many files. If a global schema change is required, handle it in a centralized migration layer or via the `GameData` versioning system implemented in `proj-v2t`.\n- **User Empowerment**: GSDL should remain library-agnostic and clean, providing the `Saveable` interface but letting the consumer decide which classes are persisted.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-28T03:14:55.336784Z","created_by":"matt","updated_at":"2026-04-28T03:31:26.720233Z","closed_at":"2026-04-28T03:25:57.058414Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-u5n","title":"Unify Rotatable Text in TextBase","description":"Refactor the text system to support rotation directly within GSDL::TextBase and its children, rendering GSDL::TextRotated obsolete. This leverages SDF font properties and the SDL_RenderTextureRotated API to ensure text remains sharp and smooth when rotated. NOTE: SDL_RenderTextureRotated must be implemented in sdl3.cr bindings first.","notes":"Refactored the text system to support rotation directly in GSDL::Text. Added rotation and origin-based center of rotation to GSDL::TextBase. Updated GSDL::Text to handle rotation by caching an internal texture when rotation is non-zero, leveraging the 'bake' method. Removed the now obsolete GSDL::TextRotated class and updated all examples to use GSDL::Text instead. Verified with examples/tween.cr and library-wide build of examples/full.cr.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T22:15:40.065054Z","created_by":"matt","updated_at":"2026-04-25T22:37:25.067821Z","closed_at":"2026-04-25T22:37:25.067653Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"proj-u5n","depends_on_id":"proj-svz","type":"blocks","created_at":"2026-04-25T22:15:42.173734Z","created_by":"matt","metadata":"{}","thread_id":""}]}
{"id":"proj-v2t","title":"Binary Persistence","description":"Goal: Transition GSDL game state persistence from plaintext JSON to a robust, compressed binary format to prevent trivial user modification and prepare for production-grade save management.\n\n## Technical Specifications\n\n### 1. Binary File Structure\nThe new format should utilize a structured binary header (12-16 bytes) before the payload:\n- Magic Number (4 bytes): 0x4753444C (ASCII 'GSDL') to verify file type.\n- Format Version (4 bytes): UInt32 to handle future migrations.\n- Checksum (4 bytes): A CRC32 or Adler32 hash of the payload to ensure data integrity.\n- Payload: The @data hash serialized to JSON, then compressed using Compress::Zlib.\n\n### 2. Implementation Requirements\n- Smart Loading: Update GSDL::GameData#load to detect the format automatically. If the first 4 bytes match the magic number, process as binary; otherwise, fallback to legacy JSON for backward compatibility.\n- Atomic Writes: Implement 'Save-and-Swap' (write to .tmp, then rename) to prevent file corruption during save operations.\n- Obfuscation: Apply a bitwise XOR transformation to the compressed payload to further discourage hex-editing.\n\n### 3. API Enhancements\n- Add GSDL::GameData#save_binary(path) and GSDL::GameData#save_json(path) for development flexibility.\n- Ensure GSDL::Data.save defaults to the new binary format.\n\n## Acceptance Criteria\n- [ ] GSDL::Data.save produces a non-human-readable file.\n- [ ] GSDL::Data.load correctly restores state from both new binary and old JSON formats.\n- [ ] Corrupted save files (invalid checksum/magic) are handled with clear error logging.\n- [ ] Verified performance: No noticeable lag during save operations for typical game state sizes.","notes":"Implemented binary persistence with Zlib compression, XOR obfuscation, and CRC32 checksums. Added Save-and-Swap atomic writes. Maintained legacy JSON support. Verified with new 'examples/save_data.cr' and full build.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T21:37:43.905964Z","created_by":"matt","updated_at":"2026-04-28T03:10:06.240729Z","closed_at":"2026-04-28T03:10:06.240550Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-v9k","title":"Refactor GSDL::Text for Baked Textures & Oversampling","description":"# Implementation Plan: Refactor GSDL::Text for Baked Textures & Oversampling\n\n## Objective\nReplace the current `SDL3::TTF::Text`-based rendering in `GSDL::Text` and `GSDL::TextBase` with a custom baked-texture approach. This will utilize oversampling to produce high-quality text scaling and rotation, rendering text to a `Surface`, converting it to a `Texture`, and drawing it using the existing `Draw#texture_rotated` batching infrastructure.\n\n## Key Files & Context\n- `src/gsdl/ui/text_base.cr`: Will be deleted.\n- `src/gsdl/ui/text.cr`: Will be completely rewritten to inherit from `Entity` directly and manage its own texture baking.\n- `src/gsdl/gfx/draw.cr`: Needs to be stripped of `DrawTextCommand`, `text_engine`, and direct `text` rendering methods.\n- `src/gsdl/ui/font.cr`: Contains the `size=` property and `render_text_blended` methods needed for oversampling.\n- Dependent UI components (`src/gsdl/ui/rich_text.cr`, `text_typed.cr`, `text_box.cr`, etc.): May need minor adjustments or commenting out *only* if they fail to compile.\n- Examples: May need commenting out *only* if they fail to compile.\n\n## Implementation Steps\n\n### 1. Task Tracking Registration\n- Create a new `br` task containing this entire plan and start the task to record the development context.\n\n### 2. Draw System Cleanup (`src/gsdl/gfx/draw.cr`)\n- Remove the `DrawTextCommand` class.\n- Remove the `def text(text : Text)` rendering methods.\n- Remove `@text_engine` and its initialization/destruction logic.\n- Remove any lingering `TextEngine` references from `Draw`.\n\n### 3. Refactor `GSDL::Text` (`src/gsdl/ui/text.cr`)\n- Delete `src/gsdl/ui/text_base.cr`.\n- Rewrite `src/gsdl/ui/text.cr`:\n  - Inherit directly from `Entity` and include `Centerable`.\n  - Add configurable property `@oversample_ratio : Float32 = 4_f32`.\n  - Retain `text`, `font`, `color`, `align`, `wrap_width`, `rotation`, `z_index`, `origin`, and `scale` properties.\n  - Implement a `bake!` method:\n    - Destroy any existing `@texture`.\n    - Skip baking if `@text` is empty.\n    - Save the original font size: `original_size = @font.size`.\n    - Temporarily scale the font: `@font.size = original_size * @oversample_ratio`.\n    - Apply font alignment (`@font.align = @align`).\n    - Render to surface: Use `@font.render_text_blended_wrapped` if `@wrap_width` is set, otherwise `@font.render_text_blended`.\n    - Convert `Surface` to `Texture` via `Game.draw.create_texture(surface)` (or `SDL3::Texture.from_surface`).\n    - Enable blend mode on the texture.\n    - Restore the original font size: `@font.size = original_size`.\n    - Destroy the surface.\n  - Call `bake!` during `initialize` and whenever relevant properties (`text`, `font`, `color`, `wrap_width`, etc.) change.\n  - Implement the `draw(draw : Draw)` method:\n    - If `@texture` exists, calculate the destination rectangle. The size should be `(texture.width / @oversample_ratio) * scale_x` and `(texture.height / @oversample_ratio) * scale_y`.\n    - Use `draw.texture_rotated` with the calculated `dest_rect`, `rotation`, `center_point_from_origin`, and `z_index`.\n  - Ensure `#destroy` cleans up the baked texture.\n\n### 4. Compilation Mitigation\n- Run `make build` or `crystal build examples/text.cr` (and others) to identify compilation failures.\n- If UI components (like `RichText`, `TextBox`) or examples fail to compile because they rely on the removed `TextBase` or `SDL3::TTF::Text` methods, modify them minimally or comment out the failing code blocks to restore compilation.\n- *Constraint*: Do not comment out code if it compiles but is functionally broken; only fix compilation errors.\n\n## Verification & Testing\n- Run `crystal run examples/text.cr` (or an equivalent text scaling example) to verify that text renders correctly, responds to scaling and rotation without pixelation, and that memory is properly managed (no texture leaks on text updates).\n- Ensure the project builds successfully without `TextEngine` or `DrawTextCommand` errors.","notes":"Refactored GSDL::Text to use baked textures with oversampling (default 4x). Removed TextBase and DrawTextCommand, simplifying the draw system. Implemented multi-line support in GSDL::Text by compositing line surfaces using a new Surface#blit method. Updated RichText and other UI components to align with the new Text architecture. Verified with examples/text.cr and examples/full.cr.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-04-26T04:09:35.575698Z","created_by":"matt","updated_at":"2026-04-26T04:29:13.275706Z","closed_at":"2026-04-26T04:29:13.275532Z","close_reason":"Completed","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-vxc","title":"Verification Task","description":"To test that your implementation is reading the kerning tables properly, create a text object displaying specific structural combinations that have dramatic structural kerning offsets in standard fonts:","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-23T03:06:47.922879Z","updated_at":"2026-05-23T03:06:53.922837Z","closed_at":"2026-05-23T03:06:53.922685Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-xdt","title":"Implement GSDL::FontFamily typography trait registry","description":"### Feature Overview\nImplement a CSS-like `GSDL::FontFamily` typography system for the GameSDL (GSDL) framework. Instead of forcing developers to manually manage and switch between separate `.ttf` paths for regular, bold, and italic variants of the same font, a `FontFamily` registry acts as a nested lookup table that maps style traits—such as `FontStyle` (Regular, Italic, Oblique) and `FontWeight` (Light, Regular, Medium, Bold, Black)—to different physical asset files.\n\nThis system will integrate seamlessly with GSDL's existing dynamic, on-demand shelf-allocating font atlas to lazily load and rasterize style-specific glyphs only when needed, maintaining optimal texture memory and high performance.\n\n---\n\n### Technical Specification & Implementation Plan\n\n#### 1. Define Typography Trait Types (`src/gsdl/ui/font_family.cr`)\nIntroduce two enums to represent font weight and style, matching CSS standards:\n\n```crystal\nmodule GSDL\n  enum FontStyle\n    Regular\n    Italic\n    Oblique\n  end\n\n  enum FontWeight\n    Light  = 300\n    Normal = 400\n    Medium = 500\n    Bold   = 700\n    Black  = 900\n  end\nend\n```\n\n#### 2. Implement the `FontFamily` Registry Class (`src/gsdl/ui/font_family.cr`)\nCreate a class that manages the mapping of a specific trait combination `(FontWeight, FontStyle)` to a loose asset path key, featuring web-standard CSS font-matching fallback rules:\n\n```crystal\nmodule GSDL\n  class FontFamily\n    getter name : String\n    @mappings = Hash({FontWeight, FontStyle}, String).new\n\n    def initialize(@name : String)\n    end\n\n    def add(weight : FontWeight, style : FontStyle, path_key : String) : Nil\n      @mappings[{weight, style}] = path_key\n    end\n\n    def resolve(weight : FontWeight, style : FontStyle) : String\n      # 1. Try exact match\n      if path = @mappings[{weight, style}]?\n        return path\n      end\n\n      # 2. Match requested style with Normal weight\n      if path = @mappings[{FontWeight::Normal, style}]?\n        return path\n      end\n\n      # 3. Match requested weight with Regular style\n      if path = @mappings[{weight, FontStyle::Regular}]?\n        return path\n      end\n\n      # 4. Fallback to any first registered font variant in this family\n      unless @mappings.empty?\n        return @mappings.values.first\n      end\n\n      raise \"FontFamily '#{name}' has no registered font mappings.\"\n    end\n  end\nend\n```\n\n#### 3. Integrate with `FontManager` (`src/gsdl/ui/font_manager.cr`)\nExtend `FontManager` to register families, keep track of them globally, and lazily load their files on demand:\n- **Registry & Mutex**: Store registered families in `@@families = Hash(String, FontFamily).new`.\n- **DSL Registration**: Add `FontManager.register_family` to support simple startup configuration:\n  ```crystal\n  def self.register_family(name : String) : Nil\n    family = FontFamily.new(name)\n    yield family\n    @@mutex.synchronize do\n      @@families[name] = family\n    end\n  end\n  ```\n- **Trait-Aware Resolution & Overloading**: Overload `FontManager.get` to resolve family names with traits:\n  ```crystal\n  def self.get(\n    name : String,\n    size : Num,\n    outline : Int32,\n    weight : FontWeight = FontWeight::Normal,\n    style : FontStyle = FontStyle::Regular\n  ) : Font\n    @@mutex.synchronize do\n      # If name is a registered family, resolve it to its style-specific path\n      if family = @@families[name]?\n        path_key = family.resolve(weight, style)\n      else\n        # 100% Backwards Compatibility: treat name as direct file path or name\n        path_key = name\n      end\n\n      resolved_name = get_name(path_key)\n      font_key = get_key(resolved_name, size, outline)\n\n      if font = @@fonts[font_key]?\n        return font\n      end\n\n      # Automatically load font data bytes on-the-fly if not already loaded\n      unless @@font_data.has_key?(resolved_name)\n        # We perform file/packfile reading directly here or defer to .load\n        # to guarantee effortless dynamic loading without prior manual load calls\n        load_font_data(path_key)\n      end\n\n      if data = @@font_data[resolved_name]?\n        font = Font.new(name: resolved_name, data: data, size: size, outline: outline)\n        @@fonts[font_key] = font\n        return font\n      end\n\n      raise \"Font data not found for path: #{path_key}\"\n    end\n  end\n  ```\n\n#### 4. Update the `Text` Entity (`src/gsdl/ui/text.cr`)\n- **Track Styling State**: Add `@font : String`, `@weight : FontWeight`, and `@style : FontStyle` as instance variables to preserve the originally requested configuration.\n- **Initialize**: Update `initialize` to accept `@weight` and `@style` parameters (defaulting to `Normal` and `Regular`), fetching the correct font atlas via `FontManager.get`.\n- **Properties & Setters**: Implement setters for `font=`, `weight=`, and `style=`. If a setting changes, fetch the correct atlas and mark the entity `@dirty = true` to force vertex cache rebuilds:\n  ```crystal\n  def weight=(weight : FontWeight)\n    if weight != @weight\n      @weight = weight\n      @font_atlas = FontManager.get(@font, font_size, 0, @weight, @style)\n      @dirty = true\n    end\n  end\n  ```\n\n#### 5. Backwards Compatibility & Verification\n- Existing files, examples, and test suites MUST continue to build and run perfectly with zero code changes (e.g. passing a loose path key like `\"fonts/PressStart2P.ttf\"` is resolved instantly).\n- Create a comprehensive interactive example `examples/font_family.cr` to demonstrate:\n  - Dynamic family registration at startup.\n  - Multi-weighted and italic text rendered on-screen simultaneously.\n  - Runtime weight and style modification demonstrating immediate on-the-fly atlas rasterization.\n\n---\n\n### Verification Plan\n\n#### Automated Verification\nRun compile-time and runtime checks on all font-related examples:\n- `make build EXAMPLE=full`\n- `make build EXAMPLE=font_size`\n- `make build EXAMPLE=text`\n- `make build EXAMPLE=text_stress`\n- `make build EXAMPLE=text_rotate_outline`\n- `make build EXAMPLE=font_family` (new example)\n\n#### Manual Verification\nVerify that the `font_family` example launches and renders perfect, high-fidelity styled glyphs at runtime without any visual glitching, overlaps, or frame stutter.\n","notes":"Accomplished: Implemented GSDL::FontFamily registry with CSS-standard trait mapping and resolution. Integrated into FontManager and Text entity. Created interactive fontFamily example. Tested successfully. Technical debt: None, maintained clean architecture. Findings: Dynamic trait resolution works seamlessly with the heap-insulated STB TrueType FFI.","status":"closed","priority":2,"issue_type":"feature","created_at":"2026-05-23T02:46:30.248179Z","created_by":"matt","updated_at":"2026-05-23T02:51:50.817121Z","closed_at":"2026-05-23T02:51:50.816840Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-yhn","title":"Enhanced Renderer Configuration","description":"Optimize SDL_Renderer for GSDL's 'Standard Indie' aesthetic, ensuring pixel-perfect sprites, integer scaling, and VSync synchronization.","notes":"Optimized SDL_Renderer for GSDL's 'Standard Indie' aesthetic. Set default scale quality to 'nearest' in GSDL::Draw. Configured logical presentation to use IntegerScale for pixel-perfect scaling. Applied vsync setting to the renderer in GSDL::Game initialization. These changes, combined with the centralization of SDF in GSDL::Font, fix the blurry text issue while maintaining the option for high-fidelity rendering.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-04-25T22:05:33.969243Z","created_by":"matt","updated_at":"2026-04-25T22:55:32.221454Z","closed_at":"2026-04-25T22:55:32.221258Z","close_reason":"done","source_repo":".","compaction_level":0,"original_size":0}
{"id":"proj-ykm","title":"Camera and Scene Drawing Refactor","description":"# Plan: Camera and Scene Drawing Refactor\n\n## Objective\nRefactor the GSDL camera and scene systems to eliminate global state dependencies, simplify coordinate management, and provide a clear distinction between \"Game World\" (Camera-aware) and \"Screen Overlay\" (Camera-ignored) drawing.\n\n## Key Changes & Context\n\n### 1. Coordinate Renaming (Standardization)\nTo avoid ambiguity, we will transition to a Godot-inspired coordinate naming scheme:\n- `x/y`: Local coordinates relative to parent.\n- `global_x/y`: Absolute coordinates within the Scene (was `scene_x/y`).\n- `render_x/y`: The actual pixel coordinate where drawing starts, applying Origin/Anchor (was `draw_x/y`).\n- `render_width/height`: The final dimensions after scaling (was `draw_width/height`).\n\n### 2. Camera-Aware `Draw` System\nThe `GSDL::Draw` class will take responsibility for camera transformations.\n- Add `camera : Camera?` property to `Draw`.\n- Implement `with_camera(camera : Camera?, &block)` to manage context.\n- Update all drawing primitives (`texture`, `rect_fill`, `point`, etc.) to automatically apply `camera.x/y` translation and `camera.zoom` if a camera is set.\n\n### 3. Scene Drawing Callbacks\n`GSDL::Scene` will provide two explicit hooks to separate world-space and screen-space content.\n- `draw_camera_view(draw)`: For objects in the scene (affected by camera).\n- `draw_screen_overlay(draw)`: For HUD/UI (ignores camera).\n- `Scene#draw` will automatically wrap these in the correct `with_camera` blocks.\n\n## Implementation Steps\n\n### Phase 1: Core Framework Updates\n1. **Update `src/gsdl/core/camera.cr`**: Ensure `Camera` doesn't have any hidden dependencies.\n2. **Update `src/gsdl/gfx/draw.cr`**:\n   - Add `@camera : Camera?`.\n   - Implement `with_camera`.\n   - Refactor `push_cmd` and primitives to handle camera math.\n3. **Update `src/gsdl/core/scene.cr`**:\n   - Define `draw_camera_view` and `draw_screen_overlay`.\n   - Update `draw` to use the new hooks and `with_camera`.\n\n### Phase 2: Coordinate Renaming (Surgical Update)\n1. **Renaming in `Entity`**: Update `scene_x/y` to `global_x/y`.\n2. **Renaming in `SpriteBase` / `Sprite` / `AnimatedSprite`**:\n   - Update `draw_x/y` to `render_x/y`.\n   - Update `draw_width/height` to `render_width/height`.\n3. **Global Search and Replace**: Use `grep_search` and `replace` to update all occurrences in the library (`src/`) and examples (`examples/`).\n\n### Phase 3: Cleanup & Optimization\n1. **Simplify Drawables**:\n   - Remove manual `Game.camera` calls from `Sprite`, `AnimatedSprite`, `Text`, `Shapes`, `TileMap`, and `HUDText`.\n   - The `draw` methods should now just pass `global_x/y` (or `render_x/y`) to the `Draw` primitives.\n2. **Update `HUD` and `HUDElement`**: Ensure they correctly default to screen-space drawing.\n\n### Phase 4: Validation\n1. **Build `examples/full.cr`**: Ensure no compile errors.\n2. **Visual Verification**:\n   - Run `examples/camera_modes.cr` (Verify zoom/translation still work).\n   - Run `examples/pause.cr` (Verify overlay doesn't move with camera).\n   - Run `examples/ui_hud.cr` (Verify HUD anchoring).\n\n## Verification Plan\n\n### Automated Tests\n- Compile check: `make build EXAMPLE=full`\n- Runtime check (5s exit): `timeout 5s make run EXAMPLE=camera_modes || true`\n\n### Manual Verification\n1. Open `camera_modes.cr`.\n2. Move the player and zoom in/out.\n3. Verify the grid and player remain aligned and the zoom is centered correctly.\n4. Verify the HUD text does NOT zoom or move.","notes":"Completed the Camera and Scene Drawing Refactor.\n- Refactored GSDL::Draw to manage camera context and handle transformations internally.\n- Refactored GSDL::Scene with draw_camera_view and draw_screen_overlay hooks.\n- Standardized coordinate naming: scene_x/y -> global_x/y, draw_x/y -> render_x/y, draw_width/height -> render_width/height.\n- Removed manual camera math from Sprite, AnimatedSprite, Shape, Text, and other drawables.\n- Updated all examples to use the new hooks and coordinate names.\n- Verified with full, camera_modes, pause, and ui_hud examples.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-05-01T22:48:22.796026Z","created_by":"matt","updated_at":"2026-05-01T23:37:20.001691Z","closed_at":"2026-05-01T23:37:20.001501Z","close_reason":"Completed","source_repo":".","compaction_level":0,"original_size":0}
