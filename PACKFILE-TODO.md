The `postinstall` script path in `shard.yml` has been corrected to `./bin/post-install.sh`. This should resolve the "command not found" error you were experiencing during `shards install`.

The fix has been applied.